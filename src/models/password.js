export default class Password {
  constructor(oldpassword, newpassword, newpasswordCheck) {
    this.oldpassword = oldpassword;
    this.newpassword = newpassword;
    this.newpasswordCheck = newpasswordCheck;
  }
}
